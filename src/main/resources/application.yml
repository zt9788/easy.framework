spring:
  # 环境 dev|test|prod
  profiles:
    active: dev #@spring.profiles.active@
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
      enabled: true
  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss
